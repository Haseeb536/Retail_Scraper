# Local development (SQLite — no PostgreSQL install required)
DATABASE_URL="file:./prisma/dev.db"
JWT_SECRET="retail-scraper-local-dev-secret-2026"

# Production (PostgreSQL) — uncomment and use postgresql provider in schema.prisma
# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/retail_scraper"
# JWT_SECRET="your-long-random-production-secret"
